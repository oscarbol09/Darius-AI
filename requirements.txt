# ==============================================================================
#  DARIUS AI — Dependencias para Windows (Asistente de Escritorio Nativo)
#  Instalar con: pip install -r requirements.txt
# ==============================================================================

# -- IA y LLMs (Llamadas nativas HTTP/REST de ultra-baja latencia) -------------
requests>=2.31.0

# -- Variables de entorno ------------------------------------------------------
python-dotenv>=1.0.0

# -- Interfaz Gráfica de Escritorio --------------------------------------------
customtkinter>=5.2.0
Pillow>=10.0.0

# -- Motores de Voz y Audio (STT / TTS) ----------------------------------------
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
edge-tts>=6.1.0
sounddevice>=0.5.0

# -- Integración con APIs de Windows y Hardware --------------------------------
pywin32>=306
comtypes>=1.2.0
pycaw>=20181226
keyboard>=0.13.5

# -- Utilidades y Procesamiento Numérico ---------------------------------------
numpy>=1.24.0
